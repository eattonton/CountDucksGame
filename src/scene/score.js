import Phaser from "phaser";
import TT from "../gameconfig";

export default class Score extends Phaser.Scene {
  constructor() {
    super({
      key: "Score",
    });
  }

  preload() {}

  create() {

        
  }
}
